import type { Meta, StoryObj } from '@storybook/angular'

import { TablePanelControlComponent } from '@vet-client/lib-control'

const meta: Meta<TablePanelControlComponent> = {
  component: TablePanelControlComponent,
  title: 'Control/TablePanel',
}
export default meta
type Story = StoryObj<TablePanelControlComponent>

export const Default: Story = {}
